#!/usr/bin/env bash
# Thin launcher so menu actions can call omatty-* by name.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export OMATTY_PLUGIN_DIR="${OMATTY_PLUGIN_DIR:-$(cd "$here/.." && pwd)}"
exec python3 "$OMATTY_PLUGIN_DIR/lib/omatty.py" "$@"
